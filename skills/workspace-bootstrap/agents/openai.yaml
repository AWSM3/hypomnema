interface:
  display_name: "Workspace Bootstrap"
  short_description: "Initialize or adopt a Hypomnema workspace"
  default_prompt: "Use $workspace-bootstrap to initialize this workspace or adopt it without moving existing content."
